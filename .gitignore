# Real config (holds your OpenAI key) lives at ~/.satrelay/config.json —
# never commit one that lands in the project folder.
config.json

# Machine-specific launchd plist, generated by install.sh
com.example.satrelay.plist

# Runtime
*.log
state.json

# Python / macOS cruft
__pycache__/
*.pyc
.DS_Store

# Transient session handoff doc
HANDOFF.md
